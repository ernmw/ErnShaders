# Shaders for OpenMW

Add this folder to your `data=` list to get the post-processors.

There's a PS wobble shader here: `resources/shaders/lib/core/vertex.glsl`, which you'll have to replace in your openmw install if you want to use it.
